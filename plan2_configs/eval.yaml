# Phase 6 — multimodal evaluation over all three pipeline stages.
defaults:
  - base

phase: eval
output: plan2_eval/full_evaluation_results.json

stages:
  phase1_sft:     plan2_models/sft_baseline
  phase3_cot_sft: plan2_models/cot_sft
  phase5_adpo:    plan2_models/adpo_final

generation:
  max_new_tokens: 64
  do_sample: false
  # InternVL's chat helper is used for generation; it handles the chat template.
