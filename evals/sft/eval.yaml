defaults:
  - base

phase: eval

data:
  randomize_options: false
  test: train_model/experiments/20pct_adaptive_v1/data/sft_test.json

stages:
  sft: train_model/experiments/20pct_adaptive_v1/models/sft

generation:
  max_new_tokens: 64
  do_sample: false

output: evals/sft/results.json
