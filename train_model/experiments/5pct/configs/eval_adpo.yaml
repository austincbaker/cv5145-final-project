defaults:
  - base

phase: eval
output: train_model/experiments/5pct/eval/results_adpo.json

data:
  randomize_options: false
  test: train_model/experiments/5pct/data/sft_test.json

stages:
  adpo: train_model/experiments/5pct/models/adpo_only

generation:
  max_new_tokens: 64
  do_sample: false
